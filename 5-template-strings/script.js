window.onload = function(){

    // Template strings do not ignore line breaks/white space:
    // var temp = `hello,
     
    // my name is genji`;

    // console.log(temp);

    // Variables can also be embedded into strings as well:
    // function logMe(name, age){
    //    console.log(`my name is ${name} and my age is ${age}`);
    // }

    // logNinja("Hanzo", "38");

    // As well as expressions:
    function logMe(name, age){
        console.log(`my name is ${name} and my age is ${32 + 6}`);
    }

    logNinja("Hanzo", "38");
}